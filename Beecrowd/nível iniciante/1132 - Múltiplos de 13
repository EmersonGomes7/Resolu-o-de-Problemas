#include <stdio.h>
#include <math.h>

int main() {
    int x,y,maior,menor,soma=0;
    scanf("%d%d",&x,&y);
    if(x>y){
        maior=x;
        menor=y;
    }else{
        maior=y;
        menor=x;
    }
    for(menor;menor<=maior;menor++){
        if(menor%13!=0)
            soma+=menor;
    }
    printf("%d\n",soma);
    return 0;
}
