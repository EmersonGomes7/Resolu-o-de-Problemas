#include <stdio.h>

int main() {
    double num,vet[100];
    scanf("%lf",&num);
    for(int i=0;i<100;i++){
        vet[i]=num;
        num/=2;
    }
    for(int i=0;i<100;i++){
        printf("N[%d] = %.4lf\n",i,vet[i]);
    }
    
    
    return 0;
}
