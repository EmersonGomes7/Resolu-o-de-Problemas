#include <stdio.h>

int main() {
    int t,n[1000],i=0;
    scanf("%d",&t);
    
    while(i<1000){
        for(int j=0;j<t;j++){
            n[i]=j;
            printf("N[%d] = %d\n",i,n[i]);
            i++;
            if(i==1000)
                break;
        }
    }
    
    return 0;
}
