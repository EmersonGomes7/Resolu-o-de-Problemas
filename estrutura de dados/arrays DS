#include <stdio.h>
int main(){
    int elementos;
    scanf("%d",&elementos);
    int vetor[elementos];
    for(int i=0;i<elementos;i++){
        fgets(&vetor[i],1,stdin);  
    }
    for(int i=elementos-1;i>=0;i--){
        printf("%d ",vetor[i]);
    }
    return 0;
}
